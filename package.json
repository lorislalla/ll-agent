{
  "name": "ll-agent",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c cyan,green \"npm run dev -w server\" \"npm run start -w client -- --proxy-config proxy.conf.json --port 4300\"",
    "build": "npm run build -w server && npm run build -w client",
    "test": "npm run test -w server",
    "test:e2e": "playwright test",
    "test:all": "npm test && npm run test:e2e",
    "start": "npm run start -w server"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0",
    "concurrently": "^9.2.1"
  }
}
